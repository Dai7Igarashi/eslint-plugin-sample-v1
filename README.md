# eslint-plugin-sample-v1

```console
$ npm install --save-dev ...

$ mkdir packages/eslint-plugin-graphql-v1
$ cd packages/eslint-plugin-graphql-v1 && npm init -y
$ npm install -w packages/eslint-plugin-graphql-v1 @graphql-eslint/eslint-plugin
$ npm install -w packages/eslint-plugin-graphql-v1 --save-dev graphql
```
